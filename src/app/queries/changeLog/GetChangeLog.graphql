query GetChangeLog {
  changeLogPosts(orderBy: logCreatedAt_DESC) {
    logCreatedAt
    logTitle
    changeLogDescription {
      html
    }
    changeLogImprovement {
      html
    }
    changeLogFix {
      html
    }
    logImage {
      url
    }
  }
}